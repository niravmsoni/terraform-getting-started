# Whats changing in V6?
- For standardizing VPC deployment, use VPC module for Terraform public registry
- Create a module for S3 buckets
    - Include load balancer permissions
    - Include instance profile permissions
